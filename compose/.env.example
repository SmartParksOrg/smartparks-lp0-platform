# smartparks-lp0-platform — environment example
# Copy to `compose/.env` (do not commit compose/.env)

APP_ENV=local
DATA_DIR=/data
UPLOAD_MAX_MB=50

SECRET_KEY=change-me-in-server
AUTH_REQUIRED=false

DATABASE_URL=sqlite:////data/app.db

BOOTSTRAP_ADMIN_EMAIL=admin@example.com
BOOTSTRAP_ADMIN_PASSWORD=change-me

REPLAY_DEFAULT_UDP_HOST=127.0.0.1
REPLAY_DEFAULT_UDP_PORT=1700

CORS_ALLOWED_ORIGINS=http://localhost:5173

SCAN_TTL_MINUTES=30
DECODE_TTL_MINUTES=30
